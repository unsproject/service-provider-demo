{
	"name": "@unsproject/demo",
	"version": "0.1.0",
	"description": "Demo",
	"main": "dist/index.js",
	"license": "ISC",
	"scripts": {
		"prepare": "husky",
		"format": "prettier --write *.json && npm run format --workspaces --if-present",
		"check-all": "npm run lint && npm run build && npm run test",
		"lint": "prettier --check *.json && npm run lint --workspaces --if-present",
		"build": "npm run build --workspaces --if-present",
		"test": "npm run test --workspaces --if-present"
	},
	"workspaces": [
		"apps/*"
	],
	"dependencies": {
		"@unsproject/common": "^0.4.0",
		"@unsproject/service-gateway": "0.1.0-rc.0",
		"express": "^4.21.2"
	},
	"devDependencies": {
		"husky": "9.1.7",
		"prettier": "3.4.2"
	},
	"engines": {
		"node": "22.12.0",
		"npm": "10.9.0"
	}
}
